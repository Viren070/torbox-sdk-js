{
  "name": "torbox-api",
  "version": "1.0.6",
  "private": true,
  "dependencies": {
    "@torbox/torbox-api": "file:../",
    "zod": "3.22.0"
  },
  "scripts": {
    "setup": "npm --prefix ../ install && npm --prefix ../ run build && npm install",
    "start": "tsc && node dist/index.js",
    "dev": "ts-node src/index.ts"
  },
  "devDependencies": {
    "ts-node": "^10.9.1",
    "typescript": "5.3.3"
  }
}
